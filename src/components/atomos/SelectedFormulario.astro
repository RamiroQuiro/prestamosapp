---

const {options,name}=Astro.props
---

<select name={name} id={name} class={`${"style"} w-full text-sm bg-primary-200/10 group-hover:ring-2 rounded-lg  border-gray-300  ring-primary-200/60 focus:ring-2  outline-none transition-colors duration-200 ease-in-out px-2 py-2`}>
    <option selected disabled class="">Seleccionar...</option>
   {
    options.map((opcion)=>{

        return(
        <option value={opcion.id} id={opcion.id} class="">{opcion.name}</option>
        )
    })
   }
  </select>
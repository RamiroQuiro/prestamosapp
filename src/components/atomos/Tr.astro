---
const{styleTr, id}=Astro.props

const href=`${Astro.url.origin}/dashboard/prestamos/listar/${id}`

---

<tr id={id} class={`${styleTr}`}>
        <slot/>
</tr>

<script define:vars={{href,id}}>
    document.getElementById(id).addEventListener('click',()=>window.location.href=href)
</script>
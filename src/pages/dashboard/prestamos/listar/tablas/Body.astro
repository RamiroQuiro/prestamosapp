---
import { Prestamo,eq, User, db, Cliente } from "astro:db";
import Tr from "../../../../../components/atomos/Tr.astro";
import TRBody from "../../../../../components/atomos/TRBody.astro";

const {arrayBody}=Astro.props
---

<tbody>
  {
    arrayBody.map((client) => {
      console.log('cada elemento ->,',client)
      return (
        <TRBody data={client} styleTr="hover:bg-primary-200/20 duration-300 cursor-pointer border-b" />
      );
    })
  }
</tbody>
